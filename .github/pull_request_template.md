## What this changes

<!-- One or two sentences: the behaviour before, the behaviour after. -->

## How it was verified

- [ ] `pnpm typecheck` and `pnpm test` pass
- [ ] If this touches how a provider CLI is invoked or parsed: run once against the real CLI.
      Name the CLI, its version and the date (see CONTRIBUTING.md, "The real-run rule").
- [ ] If this is a new architectural decision: an ADR is included under `docs/adr/`

## Notes for the reviewer

<!-- Anything a reviewer should look at first, any accepted ADR this touches, and anything written to
     ~/.braingate on the operator's behalf (scores, acceptances, quota history). -->
