# @braingate/observability

Truthful local observability for BrainGate.

This package builds sanitized pre-execution task briefs, normalized task/workflow receipts, append-only global provider quota snapshots, and deterministic dashboard snapshots. It never launches provider probes and never turns `unknown` usage into invented numbers.
